const [userLogin, setUserLogin] = useState({
  username: false,
  password: false,
});

//   if (
//     (userData.map((v) => v.name).filter((v) => v.includes(username)) ==
//     username
//       ? setUserLogin({ username: true })
//       : setUserLogin({ username: false })) &
//         (userData.map((v) => v.name).filter((v) => v.includes(password)) ==
//           password
//       ? setUserLogin({ password: true })
//       : setUserLogin({ password: false }))
//   ) {
//   }
// console.log(userLogin);
